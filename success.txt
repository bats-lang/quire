Remove all checked_* castfn usage, g1ofg0, and non-essential runtime checks.

=== HOW TO CONFIRM SUCCESS ===

1. Zero castfn usage:
   grep -rn 'checked_\|g1ofg0' src/ --include='*.bats'
   Must return zero matches.

2. No non-essential runtime comparison checks:
   Every `if` that compares an integer (other than a recursion stopping
   condition like `if fuel = 0 then ...`) must either:
   (a) be on external input and return a result type with error details, or
   (b) be a recursion stopping condition.
   Verify by reading the diff and confirming each remaining `if` is one of
   these two categories.

3. No new functions added to arith:
   diff the arith package — it must be unchanged.

4. Error handling surfaces details:
   Functions that validate external input (file sizes, ZIP offsets, XML
   positions) must return result types. Errors must eventually reach the
   user with a message describing what went wrong (e.g. "invalid ZIP:
   file too large", "EPUB metadata offset out of bounds").

5. bats check passes locally:
   bats check --repository ../repository-prototype
   Must exit 0 (WASM-only module patsopt errors in native build are
   expected and don't fail the check).

6. All changes on a feature branch with a PR, per CLAUDE.md workflow.

7. PR CI is green (all checks pass).

8. PR is merged to main.
